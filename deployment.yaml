apiVersion: apps/v1
kind: Deployment
metadata:
  name: sms-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sms
  template:
    metadata:
      labels:
        app: sms
    spec:
      containers:
      - name: sms
        image: sms-app
